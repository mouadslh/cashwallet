# Cash Wallet Frontend

Tech: React + Vite, Material-UI, Recharts, React Router, Notistack

## Setup

1. Copy `.env.example` to `.env`
2. Install and run

```bash
cd frontend
npm install
npm run dev
```